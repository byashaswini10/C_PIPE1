pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh '''
					#!/bin/bash
					echo "check 1"
					git checkout c_repo
					make
					'''
            }
        }
    }
}

